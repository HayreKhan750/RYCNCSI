rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    function isSignedIn() { return request.auth != null; }
    match /users/{uid}/{allPaths=**} {
      allow read: if true;
      allow write: if isSignedIn() && request.auth.uid == uid;
    }
    match /instructors/{iid}/{allPaths=**} {
      allow read: if true;
      allow write: if isSignedIn() && request.auth.uid == iid;
    }
  }
}
